{{app-navbar sidebar=sidebar authors=authors}}
<div class="container {{if sidebar '' 'hidden-sidebar'}}">
  <aside>
    {{app-sidebar}}
  </aside>
  <section class="body">
    <div class="container">
      {{liquid-outlet class="toleft"}}
    </div>
  </section>
</div>
{{ember-notify}}
